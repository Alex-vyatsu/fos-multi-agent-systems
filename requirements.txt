# Минимальный воспроизводимый контур не требует сторонних пакетов.
# Квалифицированный runtime: CPython 3.12.13; код совместим с Python 3.11+.
#
# Библиотеки рекомендуемого/расширенного профиля не добавляются сюда:
# преподаватель выпускает отдельный семестровый lock-файл после clean run.
# См. docs/resource-profiles.md и resources/software/python-libs/README.md.

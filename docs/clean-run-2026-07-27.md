# Протокол чистого запуска — 27 июля 2026 года

**Объект:** рабочая ветка итерации 1.2.0  
**Профиль:** минимальный CPU-only  
**Qualification ID:** `clean-run-20260727-minimal-03`  
**Статус:** `pass` в указанной границе

## Окружение

| Параметр | Значение |
|---|---|
| Исходники | отдельная копия рабочего дерева `/tmp/fos-clean-run.final.FxoyQl/source` без `.git`, кешей и виртуальных окружений; временный каталог после проверки не является публикуемым артефактом |
| Python | новое `venv` из CPython 3.12.13 |
| OS | Darwin 25.3.0 arm64 |
| CPU / RAM | Apple M1 Pro / 16 ГБ |
| GPU | не используется |
| Сеть / API / секреты | не требуются |
| Выполнившая роль | технический исполнитель итерации; `REPRODUCIBILITY_OWNER` организационно не назначен |

## Команды и результаты

Копия и окружение создавались без ручной правки исходников:

```bash
rsync -a --exclude .git --exclude __pycache__ --exclude .venv \
  <repository>/ <clean>/source/
python3.12 -m venv <clean>/venv
cd <clean>/source
<clean>/venv/bin/python other/tools/check_all.py
for seed in 7 42 99; do
  <clean>/venv/bin/python examples/m2_contract_net.py --seed "$seed" --tasks 8
  <clean>/venv/bin/python examples/m5_independent_q.py --seed "$seed" --episodes 2000
done
```

| Проверка | Результат | Время `real` |
|---|---|:--:|
| Внутренние ссылки и якоря | 0 битых ссылок, 0 битых якорей | в составе общего запуска |
| Веса рубрик | 17 рубрик, 0 расхождений | в составе общего запуска |
| Трассировка КРМ и ресурсов | 9 индикаторов, 15 локальных ресурсов, 0 ошибок | в составе общего запуска |
| Инвентарь | 23 КИМ, 17 рубрик, 27 обязательных файлов | в составе общего запуска |
| Unit/contract tests | 14 тестов, все `OK` | в составе общего запуска |
| `check_all.py` целиком | `pass` | 0.24–0.32 с в двух последовательных прогонах |
| M2 Contract Net, seeds 7/42/99, 8 задач | в каждом прогоне `task_count = 8`, `message_count = 40` | 0.02 с на seed |
| M5 IQL, seeds 7/42/99, 2000 эпизодов | coordination rate 0.965–0.990, согласованная политика на каждом seed | 0.02 с на seed |
| Экзаменационный normal path | `passed = true` | 0.04 с |
| Экзаменационный tool failure + recovery | `failed → completed`, `passed = true` | 0.04 с |
| Поиск `.env`, `.pem`, `.key` и token-like `sk-*` | совпадений нет | вне измерения |

## Негативные и recovery-сценарии

Автоматически подтверждены:

- отсутствующий dataset даёт явную команду восстановления генератором;
- неизвестная capability и недоступный агент отклоняются до делегирования;
- неподдерживаемая версия A2A отклоняется;
- истёкший grant завершает задачу ошибкой;
- MCP tool вне scope и недоступный tool не создают артефакт;
- cancel переводит незавершённую задачу в `canceled`;
- повтор с тем же effect key возвращает один артефакт без двойного эффекта,
  но повторно проверяет scope и остальные границы grant;
- экзаменационный отказ инструмента восстанавливается отдельным безопасным
  прогоном.

Ручных исправлений внутри чистой копии не выполнялось.

## Граница вывода

Протокол относится только к минимальному профилю на указанном окружении. Он не
подтверждает Docker build, рекомендуемый/расширенный профиль, wire-conformance
A2A/MCP или качество реальной LLM. Docker CLI доступен, но daemon в среде
итерации недоступен, поэтому контейнерный рецепт остаётся структурно
проверенным, но не квалифицированным запуском. Повторная квалификация нужна
после изменения примеров, tests, manifest, runtime или Docker base image.
